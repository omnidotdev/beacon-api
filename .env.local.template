# beacon-api Environment Configuration

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/beacon

# Auth (JWT validation from HIDRA)
AUTH_BASE_URL=https://identity.omni.dev
AUTH_SECRET=  # Generate: openssl rand -base64 32

# Encryption (for provider keys)
# Generate: openssl rand -base64 32
ENCRYPTION_KEY=

# Gateway
GATEWAY_URL=http://localhost:18790
GATEWAY_SECRET=  # For signing session tokens

# Billing (optional)
# AETHER_API_URL=https://api.billing.omni.dev
# AETHER_API_KEY=

# Server
PORT=4000
NODE_ENV=development
